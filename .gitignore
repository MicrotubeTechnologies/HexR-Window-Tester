# PyInstaller / Inno Setup output
build/
dist/
Installer/
*.spec.bak

# Generated at build time from tools/make_icon.py
assets/icon-preview.png

__pycache__/
*.py[cod]

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/out/
*.apk
*.aab

# Signing material for the Play Store upload key. The keystore and its
# passwords never belong in a repository - losing the key is recoverable
# through Play App Signing, leaking it is not.
android/keystore.properties
*.jks
*.keystore

# Design canvas: the .dc.html artboards are the source; the seeded page is a
# ~2 MB build output that is regenerated from them.
design/hexr-tester-android.html
