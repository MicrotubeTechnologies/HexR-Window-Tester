# Play Console listing — HEXR Tester 1.0.0

Everything the Console asks for, written out once so the fields can be pasted
rather than reinvented at the keyboard. Character limits are Google's; the
counts in brackets are what the text below actually is.

Assets live beside this file and are regenerated by `tools/make_play_assets.py`:

| Console field | File | Spec |
|---|---|---|
| App icon | `icon-512.png` | 512x512, 32-bit PNG, under 1 MB |
| Feature graphic | `feature-graphic-1024x500.png` | 1024x500, 24-bit PNG, no alpha |
| Phone screenshots | `screenshots/phone/*.png` | 1080x1920, at least 2, 4+ recommended |

`preview.png` is a contact sheet for eyeballing the icon against a launcher
render. It is not uploaded anywhere.

---

## Store listing

### App name — 30 max [11]

```
HEXR Tester
```

### Short description — 80 max [77]

```
Bluetooth bench tester for HEXR haptic gloves — drive, measure, pass or fail.
```

### Full description — 4000 max [2873]

```
HEXR Tester connects to a HEXR pneumatic haptic glove over Bluetooth LE and tells you whether it works.

It is the phone version of the HEXR Window Tester, and it speaks the same wire protocol against the same verdict thresholds — so a glove that passes on a bench laptop passes here, and a result recorded on either means the same thing.

THREE QUESTIONS IT ANSWERS

• Does this glove work? Run the quick test and read the verdicts.
• What does this setting feel like? Drive a channel and put your hand in it.
• Why is this one weak? Watch the live pressure while it fires.

CONNECT

Scan and connect the left glove, the right, or both at once — they are separate Bluetooth devices and the app drives them independently. Each one shows its battery, its source pressure, its address and the negotiated MTU, and says whether telemetry is actually streaming.

TEST

A tappable diagram of the glove stands in for a list of channel names. Select any of the six channels — thumb, index, middle, ring, pinky, palm — set intensity from 0 to 100%, and choose steady pressure or vibration from 1 to 20 Hz. Trigger, and each channel's live pressure fills its own shape in the diagram, so a weak channel is visibly paler than a healthy one at a glance. Release all vents every channel, whatever happens to be selected.

QUICK TEST

The canned sweep: every channel driven to full for two seconds, with peak pressure and time-to-peak recorded for each. Every channel comes back Good, Poor or Fail under one plain-English headline — and a dead pump is reported as one fault in the pump, not as six failed channels, so nobody goes replacing the wrong part.

• Good — above 40 kPa, full pressure
• Poor — 2 to 40 kPa, moved but never reached full
• Fail — under 2 kPa, not working

SAFE BY DEFAULT

Channels vent when you leave the Test screen, when a glove disconnects, and the moment the app goes to the background. A phone gets pocketed mid-test in a way a bench laptop does not, and a channel left inflated presses on someone's hand until the battery dies.

NO ACCOUNT, NO NETWORK

The app holds no internet permission at all, so the operating system will not let it open a connection even if it tried. It collects nothing, sends nothing, and keeps nothing after you close it. Everything happens between the phone and the glove in front of you.

WHAT YOU NEED

Android 8.0 or newer, Bluetooth LE, and a HEXR glove. The glove is the whole point — without one there is nothing for the app to connect to.

This is a test and diagnostic instrument, not a runtime. HEXR gloves have no consumer companion app and do not pair through Android's Bluetooth settings. To build something with HEXR you integrate the com.microtube.hexr Unity package or the Python library; the glove does not need this app to work with your application.

HEXR and HEXR Tester are products of Microtube Technologies.
```

### What's new — 500 max [292]

```
First release of HEXR Tester for Android.

• Connect a left glove, a right glove, or both over Bluetooth LE
• Drive any of the six channels: steady pressure or 1-20 Hz vibration
• Quick test sweeps all six and returns Good, Poor or Fail per channel
• Live pressure on a tappable glove diagram
```

---

## Screenshots

Upload in this order. The first one is what shows in search results, so the
glove diagram mid-drive leads: it is the only image that explains what the app
is for without reading anything.

| # | File | What it shows |
|---|---|---|
| 1 | `02-test-driving.png` | Six channels lit at varying pressure, Trigger showing Active |
| 2 | `03-quick-test-pass.png` | Green "All 6 channels good" over the results table |
| 3 | `04-quick-test-fail.png` | Red "1 of 6 channels failed" with the diagnostic paragraph |
| 4 | `01-connect.png` | Both gloves connected and streaming, battery and source tiles |
| 5 | `05-splash.png` | The HEXR lockup |

Tablet screenshots are deliberately not supplied. The app is authored against a
390x844 dp phone; `TestScreen` is a fixed-height layout with no scroll and has
had no large-screen design pass, so a tablet screenshot would be an honest
picture of a layout nobody designed.

---

## App content declarations

### App access — the one that gets apps rejected

**All or some functionality is restricted.** Every screen in this app is a view
onto a glove. A reviewer without one sees a scan that finds nothing and dashes
where the readings should be, and "we could not test the app" is a rejection,
not a question.

Provide, in the App access section:

- Instructions saying the app requires HEXR pneumatic haptic glove hardware
  from Microtube Technologies, that the hardware is not publicly purchasable,
  and that the app is a service tool for technicians.
- **A screen recording made on a real phone with a real glove**, showing scan,
  connect, a channel driven, and a quick test running to a verdict. This is the
  one asset the emulator cannot stand in for, and it is what turns a blocked
  review into a passed one.

No login credentials exist to supply — there are no accounts.

### Data safety

| Question | Answer |
|---|---|
| Does your app collect or share any of the required user data types? | **No** |
| Is all of the user data collected by your app encrypted in transit? | n/a — nothing is collected |
| Do you provide a way for users to request that their data is deleted? | n/a — nothing is collected |

Backed by the manifest: there is no `INTERNET` permission, so the app cannot
open a network connection, and `allowBackup="false"` keeps the OS from copying
anything off the device. Telemetry lives in memory for the life of the process.

### Privacy policy

```
https://microtubetechnologies.github.io/HexR-Window-Tester/privacy.html
```

Served by GitHub Pages from `docs/` on `main` — see `docs/privacy.html`. It only
goes live once this work is merged to `main` **and** Pages is switched on at
Settings → Pages → Deploy from a branch → `main` → `/docs`.

### Permissions

Nothing here needs a declaration form, but the Console will ask you to justify
the Bluetooth pair:

- `BLUETOOTH_SCAN` carries `neverForLocation`, so the app tells Android it will
  not derive location from scan results.
- `BLUETOOTH_CONNECT` is what talks to the glove.
- `BLUETOOTH`, `BLUETOOTH_ADMIN` and `ACCESS_FINE_LOCATION` are all capped at
  `maxSdkVersion="30"`. Before Android 12 there were no dedicated Bluetooth
  permissions and location was the only way the platform allowed a scan at all.
- `android.hardware.location` is declared `required="false"` on purpose. The
  legacy location permission would otherwise imply a *required* location
  feature, and Play would quietly hide the app from devices without one.

### Content rating

A utility with no user-generated content, no communication features, no
purchases and no ads. The questionnaire should come out at the lowest rating in
every region. Category: **Utility / Productivity / Communication / Other**.

### Target audience

18 and over. It is a professional service tool for technicians, not directed at
children, and there is nothing in it aimed at a younger audience.

### Ads

**No ads.**

### Other declarations

| Declaration | Answer |
|---|---|
| Government app | No |
| Financial features | No |
| Health apps | No — the glove is haptics hardware, not a medical device |
| News app | No |
| COVID-19 contact tracing | No |
| Data safety: device or other IDs | Not collected |

---

## Release track

Start on **Internal testing**, install from the Play link on a phone that has a
glove to hand, and confirm the store build behaves like the sideload before
promoting it. The store build is signed with a different key and carries a
different package name from the technician build (`.debug` suffix), so both can
sit on one phone and be compared side by side.

Note on the 12-testers-for-14-days closed-testing requirement: that applies to
**personal** developer accounts opened after November 2023. An organisation
account — which is what the MicrotubeTechnologies account should be — is exempt.
Check which one the Console is showing before planning a rollout around it.
